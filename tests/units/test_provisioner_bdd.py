"import os\nimport sys\nimport time\nimport pytest\nimport httpx\nfrom pathlib import Path\nfrom pytest_bdd import scenarios, given, when, then, parsers\n\n# Setup python path to import server modules\nPROJECT_ROOT = Path(__file__).resolve().parent.parent.parent\nsys.path.append(str(PROJECT_ROOT / \"server\"))\n\nfrom vm_test_helper import VMTestHelper\nfrom effects import (\n    PipelineStarted,\n    BudgetSet,\n    QueueJob,\n    VMAllocated,\n    VMDeallocated,\n    JobStarted,\n    JobCompleted,\n)\nfrom event_store import EventStore\n\n# Load scenarios\nscenarios('features/provisioner_lifecycle.feature')\n\n@pytest.fixture(scope=\"module\", autouse=True)\ndef vm_helper():\n    helper = VMTestHelper(vm_name=\"test-provisioner-vm\", agent_port=8003, worker_port=8880)\n    helper.start_gsa()\n    helper.boot_vm()\n    helper.setup_ssh_tunnel()\n    helper.setup_mock_vastai()\n    helper.start_worker_in_vm()\n    helper.start_agent_in_vm(\"provisioner\")\n    yield helper\n    # helper.cleanup()\n\n\n@pytest.fixture\ndef run_id():\n    return f\"run_{int(time.time() * 1000)}\"\n\n@pytest.fixture\ndef event_store():\n    store = EventStore(log_dir=\"/Users/orpington/documentary-pipeline\")\n    return store\n\ndef clear_local_event_store():\n    import glob\n    for file in glob.glob(\"/Users/orpington/documentary-pipeline/events_*.db*\"):\n        try:\n            os.remove(file)\n        except Exception:\n            pass\n\n@given('the GSA event store contains a queued \"tts\" job')\ndef step_queued_tts_job(run_id, event_store):\n    clear_local_event_store()\n    event_store.append(run_id, PipelineStarted(run_id=run_id, agent=\"operator\"), \"\")\n    event_store.append(run_id, BudgetSet(run_id=run_id, agent=\"operator\", budget_usd=10.0), \"\")\n    \n    event_store.append(run_id, QueueJob(\n        run_id=run_id,\n        agent=\"audio\",\n        job_id=\"job_tts_1\",\n        job_type=\"tts\",\n        scene_num=1,\n        block_id=\"s1_v1\",\n        slot_id=\"s1_v1\",\n        params={\"text\": \"Th
<truncated 4034 bytes>