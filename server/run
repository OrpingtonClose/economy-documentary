#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")"
source .env
export VAST_API_KEY
rm -rf /tmp/documentary-pipeline
# Agent memory is NOT wiped — it persists across runs in server/agent_memory/
/opt/homebrew/bin/python3 -m strands_agents.run_strands "$@" --api-key "$API_KEY"
