"""strands-agents-evals harness for the documentary pipeline.

One :class:`strands_evals.Experiment` per component lives in
``experiments/``. Custom :class:`strands_evals.Evaluator` subclasses
live in ``evaluators/``. ``ToolSimulator`` + ``ActorSimulator`` configs
live in ``simulators/``.
"""
